#include "../../include/comm/CentralChannel.h"
#include <iostream>

void SMI::Comm::CentralChannel::send(channel_data buf, peer_num dest) {
    std::cout << "Central Channel" << std::endl;
}

void SMI::Comm::CentralChannel::recv(channel_data buf, peer_num dest) {

}
